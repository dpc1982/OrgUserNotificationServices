{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "prefix": "user-",
      "key": "Primary",
      "keyLength": 12,
      "description": "Auto-generated primary key (user-<12 digits>)"
    },
    "firstName": {
      "type": "string",
      "description": "Given name"
    },
    "lastName": {
      "type": "string",
      "description": "Family name"
    },
    "dateOfBirth": {
      "type": "string",
      "description": "Date of birth as an RFC 3339 full-date (yyyy-MM-dd, zero-padded); a time component is rejected",
      "format": "date"
    },
    "gender": {
      "type": "string",
      "description": "Gender; free string validated against the shared reference list at the caller layer"
    },
    "email": {
      "type": "string",
      "description": "Email address"
    },
    "phoneNumber": {
      "type": "string",
      "description": "Contact phone number"
    },
    "orgId": {
      "type": "string",
      "description": "Organisation id this user belongs to (org catalogue reference)"
    },
    "orgName": {
      "type": "string",
      "description": "Display name of the organisation referenced by orgId"
    },
    "functionalRole": {
      "type": "string",
      "description": "Functional role assigned to the user for the referenced organisation/catalogue"
    },
    "password": {
      "type": "string",
      "description": "Hashed password (hashed by the portal backend before submission; source of truth)"
    },
    "pin": {
      "type": "string",
      "description": "Hashed PIN (hashed by the portal backend before submission; source of truth)"
    },
    "location0Id": {
      "type": "string",
      "description": "Identifier for the top-level (level 0) node of the location hierarchy"
    },
    "location0Name": {
      "type": "string",
      "description": "Display name for the top-level (level 0) node of the location hierarchy"
    },
    "location1Id": {
      "type": "string",
      "description": "Identifier for the level 1 node of the location hierarchy, nested under location0"
    },
    "location1Name": {
      "type": "string",
      "description": "Display name for the level 1 node of the location hierarchy"
    },
    "location2Id": {
      "type": "string",
      "description": "Identifier for the level 2 node of the location hierarchy, nested under location1"
    },
    "location2Name": {
      "type": "string",
      "description": "Display name for the level 2 node of the location hierarchy"
    },
    "location3Id": {
      "type": "string",
      "description": "Identifier for the level 3 node of the location hierarchy, nested under location2"
    },
    "location3Name": {
      "type": "string",
      "description": "Display name for the level 3 node of the location hierarchy"
    },
    "location4Id": {
      "type": "string",
      "description": "Identifier for the level 4 node of the location hierarchy, nested under location3"
    },
    "location4Name": {
      "type": "string",
      "description": "Display name for the level 4 node of the location hierarchy"
    },
    "displayName": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional display label for the user's functional role (e.g. 'FIELD_OFFICER'). Nullable"
    },
    "createdBy": {
      "type": "string",
      "description": "User id of the creator (set by the portal/auth layer)"
    },
    "updatedBy": {
      "type": "string",
      "description": "User id of the last updater (set by the portal/auth layer)"
    }
  },
  "required": [
    "firstName",
    "lastName",
    "email",
    "orgId",
    "functionalRole",
    "password",
    "pin"
  ]
}